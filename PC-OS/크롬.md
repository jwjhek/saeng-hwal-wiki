---
publish: true
draft: false
---

# 크롬

> **분류:** PC·OS · [[생활위키 목차]]

Google Chrome은 Chromium 기반 **웹 브라우저**다. 검색·웹앱·개발자 도구·동기화가 한곳에 있어 PC 일상의 기본 창구인 경우가 많다.  
Windows에서는 보통 [[윈도우 사용법]] 위에 올린다. Edge·Brave 등도 같은 계열이면 단축키·설정 감각이 비슷하다.

공식: [https://www.google.com/chrome/](https://www.google.com/chrome/)  
다운로드는 **공식 사이트**만 사용한다.

확인일: 2026-08-06  
메뉴 이름은 Chrome 버전·언어에 따라 조금 다를 수 있다.

---

## 1. 설치·기본 앱

1. 공식에서 설치 → Google 계정으로 로그인(선택)  
2. Windows: `설정` → `앱` → `기본 앱` → Chrome을 기본 브라우저로 ([[윈도우 사용법]])  
3. 주소창에 `chrome://version` — 버전·경로 확인  

모바일 Chrome은 [[갤럭시 폰과 워치 사용법]] 폰에서 Play 스토어로. PC와 **같은 Google 계정**이면 탭·북마크 동기화가 된다.

---

## 2. 화면 구성

| 요소 | 역할 |
|------|------|
| 주소창(옴니박스) | URL 입력 + 검색 |
| 탭 | 페이지 단위. 드래그로 분리·합치기 |
| 북마크 바 | `Ctrl` + `Shift` + `B` 로 표시 |
| 확장 프로그램 아이콘 | 퍼즐(확장) → 핀 고정 |
| 프로필 아이콘 | 계정·프로필 전환 |
| ⋮ 메뉴 | 설정, 방문 기록, 다운로드 등 |

특수 주소:

| 주소 | 내용 |
|------|------|
| `chrome://settings` | 설정 |
| `chrome://extensions` | 확장 |
| `chrome://bookmarks` | 북마크 관리 |
| `chrome://history` | 방문 기록 |
| `chrome://downloads` | 다운로드 |
| `chrome://flags` | 실험 기능 (조심) |
| `chrome://gpu` | GPU·하드웨어 가속 |

---

## 3. 필수 단축키 (Windows)

### 탭·창

| 단축키 | 동작 |
|--------|------|
| `Ctrl` + `T` | 새 탭 |
| `Ctrl` + `W` | 탭 닫기 |
| `Ctrl` + `Shift` + `T` | 닫은 탭 다시 열기 |
| `Ctrl` + `Tab` / `Ctrl` + `Shift` + `Tab` | 다음·이전 탭 |
| `Ctrl` + `1`~`8` | n번째 탭 |
| `Ctrl` + `9` | 마지막 탭 |
| `Ctrl` + `N` | 새 창 |
| `Ctrl` + `Shift` + `N` | 시크릿 창 |
| `Ctrl` + `Shift` + `A` | 탭 검색(최근 Chrome) |

### 탐색·페이지

| 단축키 | 동작 |
|--------|------|
| `Ctrl` + `L` / `Alt` + `D` | 주소창 포커스 |
| `Alt` + `←` / `→` | 뒤로·앞으로 |
| `Ctrl` + `R` / `F5` | 새로고침 |
| `Ctrl` + `Shift` + `R` | 강력 새로고침(캐시 무시) |
| `Ctrl` + `F` | 페이지 내 찾기 |
| `Ctrl` + `+` / `-` / `0` | 확대·축소·기본 |
| `Space` / `Shift` + `Space` | 스크롤 아래·위 |
| `Home` / `End` | 맨 위·맨 아래 |

### 북마크·도구

| 단축키 | 동작 |
|--------|------|
| `Ctrl` + `D` | 북마크 추가 |
| `Ctrl` + `Shift` + `O` | 북마크 관리자 |
| `Ctrl` + `H` | 방문 기록 |
| `Ctrl` + `J` | 다운로드 |
| `Ctrl` + `Shift` + `Delete` | 인터넷 사용 기록 삭제 |
| `F12` / `Ctrl` + `Shift` + `I` | 개발자 도구 |
| `Ctrl` + `U` | 소스 보기 |
| `Ctrl` + `P` | 인쇄 / PDF로 저장 |
| `Ctrl` + `S` | 페이지 저장 |

---

## 4. 프로필·동기화

| 개념 | 용도 |
|------|------|
| **프로필** | 북마크·확장·로그인을 사람/용도별로 분리 (개인 / 회사) |
| **동기화** | Google 계정으로 북마크·암호·확장 등을 기기 간 맞춤 |

설정 → 나와 Google → 동기화 및 Google 서비스.  
암호·결제 수단은 동기화 항목을 선택적으로 끈다.

팁: 회사 PC와 개인을 **프로필로 나누면** 확장·쿠키가 섞이지 않는다.  
시크릿(`Ctrl` + `Shift` + `N`)은 창을 닫으면 그 세션 쿠키·기록이 남지 않는 쪽에 가깝다(다운로드·북마크 추가는 남을 수 있음).

---

## 5. 북마크·다운로드·검색

### 북마크

- 폴더로 주제별 정리  
- 북마크 바에는 **매일 쓰는 것만**  
- 백업: 북마크 관리자 → ⋮ → 북마크 내보내기(HTML)  

### 다운로드

- 기본 폴더: 보통 `다운로드` ([[윈도우 사용법]] 파일·폴더)  
- 설정 → 다운로드에서 위치·다운로드 전 저장 위치 묻기  
- `chrome://downloads` 또는 `Ctrl` + `J`  

### 검색엔진

설정 → 검색엔진 → 주소창에 사용된 검색엔진.  
사이트별 검색: 검색엔진 관리에서 키워드 지정(예: `wiki` → 위키 검색).

---

## 6. 확장 프로그램

`chrome://extensions` 또는 Chrome 웹 스토어.

| 할 일 | 메모 |
|------|------|
| 설치 | 스토어에서 추가 → 권한 확인 |
| 핀 | 툴바에 고정 |
| 끄기/제거 | 확장 페이지에서 |
| 사고 대응 | 수상한 확장은 즉시 제거 + 암호 변경 |

원칙:

1. **최소 설치** — 탭·CPU·개인정보 비용  
2. 권한(모든 사이트 데이터 읽기)이 과하면 재고  
3. 스토어 평점·게시자·최근 업데이트 확인  
4. 애드블록·비밀번호 관리자·번역 정도가 실사용 상위권  

개발·위키: 불필요하면 끄고, [[Cursor 사용법]]·에디터와 역할을 나눈다.

---

## 7. 비밀번호·자동완성·안전

1. Google 비밀번호 관리자 또는 외부 비밀번호 매니저  
2. 설정 → 자동 완성 — 주소·결제 저장 여부  
3. **유출 알림**이 있으면 해당 사이트 암호 변경  
4. HTTPS·자물쇠 아이콘 — 민감 정보는 HTTP에 치지 않기  
5. 가짜 주소(호모그래프)·급한 “계정 정지” 메일 경계  
6. 확장·테마는 공식 스토어 위주  

사이트 데이터 삭제: `Ctrl` + `Shift` + `Delete` → 기간·쿠키·캐시 선택.  
한 사이트만: 주소창 자물쇠/튜닝 아이콘 → 사이트 설정 → 데이터 삭제.

---

## 8. 성능·탭 관리

| 증상 | 시도 |
|------|------|
| 느림·팬 소음 | 탭·확장 줄이기, 작업 관리자(`Shift` + `Esc`) |
| 메모리 | 안 쓰는 프로필·탭 그룹으로 접기 |
| 깨진 페이지 | 강력 새로고침, 캐시·쿠키 삭제, 하드웨어 가속 on/off |
| 영상 끊김 | 하드웨어 가속, GPU 드라이버 ([[윈도우 사용법]] 성능) |

탭 그룹: 탭 우클릭 → 탭을 새 그룹에 추가. 색·이름으로 업무/쇼핑 분리.  
검색으로 탭 찾기: `Ctrl` + `Shift` + `A` (지원 버전).

Chrome 작업 관리자: `Shift` + `Esc` — 어떤 탭·확장이 무거운지 종료.

---

## 9. 개발자 도구 (맛보기)

`F12` — 프론트·웹 디버깅 기본기. [[Java 언어 학습]]·[[Spring과 Spring Boot 학습]] 웹 화면 확인할 때도 씀.

| 패널 | 용도 |
|------|------|
| Elements | HTML·CSS 검사 |
| Console | 로그·간단한 JS |
| Network | 요청·상태코드·느린 API |
| Application | 쿠키·스토리지 |
| Lighthouse | 성능·접근성 감사(개요) |

장치 툴바: `Ctrl` + `Shift` + `M` — 모바일 폭 미리보기.

---

## 10. 모바일·다른 Google 서비스

| 연결 | 메모 |
|------|------|
| Android Chrome | 계정 동기화, 데스크톱으로 보내기 |
| 갤럭시 | 기본 브라우저를 Chrome으로 둘지 Samsung Internet과 선택 |
| YouTube 등 | 웹·앱 — [[유튜브]] |
| AI·웹앱 | Gemini·ChatGPT 등은 탭에서 — [[현존 AI 비교]] |

---

## 11. Edge / 다른 Chromium과

| | Chrome | 참고 |
|--|--------|------|
| 엔진 | Chromium | Edge·Brave·Whale 등도 유사 |
| 계정 | Google | Edge는 Microsoft |
| 선택 | 확장 생태계·습관 | 회사 정책상 Edge 강제일 수 있음 |

단축키 상당수가 같다. “크롬만의 기능”보다 **프로필·동기화·확장**이 체감 차이다.

---

## 12. 일상 체크리스트

- [ ] 공식 설치 + (원하면) Google 로그인·동기화  
- [ ] 기본 브라우저 지정  
- [ ] 개인/업무 프로필 분리  
- [ ] 북마크 바·폴더 정리  
- [ ] 확장 다이어트 + 핀만 남기기  
- [ ] `Ctrl` + `Shift` + `T`, `Ctrl` + `L`, `Ctrl` + `Shift` + `N` 손에 익히기  
- [ ] 비밀번호 유출 알림·2단계 인증(Google 계정)  

---

## 관련

- [[생활위키 목차]]
- [[윈도우 사용법]]
- [[갤럭시 폰과 워치 사용법]]
- [[유튜브]]
- [[현존 AI 비교]]
- [[Cursor 사용법]]
