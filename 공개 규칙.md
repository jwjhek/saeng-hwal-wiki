---
publish: false
draft: false
aliases:
 - 퍼블리시
 - Quartz 공개
tags:
 - meta
---

# 공개 규칙

이 vault는 **Obsidian 로컬 위키**이면서, Quartz로 **골라서 웹에 공개**한다. 
**사이트에는 올리지 않는다** — vault·작성자용 메타 문서.

> **분류:** 메타 · [[생활위키 목차]]

확인일: 2026-08-11

---

## 1. 목적

| 층 | 역할 |
|----|------|
| Obsidian | 모든 메모 (일상·가계·기기·학습) |
| Quartz 사이트 | `publish: true` 인 글만 |
| `private/` | Git에도 안 올리는 민감 메모 (`.gitignore`의 `private/`) |

---

## 2. frontmatter

노트 맨 위 YAML:

```yaml
---
publish: true   # 사이트에 포함 (명시적 공개)
draft: false    # true면 빌드에서 제외
depth: article # hub | article | stub (기본 article)
refresh: weekly # (선택) 주간 점검 글 — 최저가·시즌 세일
tags:
 - 생활
---
```

| 필드 | 의미 |
|------|------|
| `publish: true` | **공개**. ExplicitPublish가 이것만 통과 |
| `publish` 없음 / `false` | **비공개**(사이트 미포함). vault·Git에는 남을 수 있음 |
| `draft: true` | 초안. 공개 표시여도 제외 |
| `depth: hub` | MOC·허브 — **생활위키 목차**에 한 줄 등록 |
| `depth: stub` | 짧은 글 — 목차 생략, 상위 허브 표만 |
| `depth: article` | 일반 글 (기본). 허브 아래면 목차 생략 가능 |
| `refresh: weekly` | 확인일·표 **매주** 갱신 전제 ([[최저가와 쿠폰]]) |

**기본 방침:** 새 글은 `publish: false` 또는 필드 생략 → 다듬은 뒤 `true`로 올린다. 
지금은 학습·사용법 글 대부분을 `true`로 두었다. 개인 정보·증권·계좌가 들어가면 즉시 `false` 또는 `private/`.

---

## 3. 어디에 두나

```text
생활위키/ ← Obsidian vault = Quartz -d .
├── index.md ← 사이트 홈 (publish: true)
├── 생활위키 목차.md ← 목차
├── 공개 규칙.md ← 이 글
├── PC-OS/ · 작성-지식/ … ← 분류 폴더
├── templates/ ← 템플릿 (빌드 ignore)
├── private/ ← 민감 (gitignore, 빌드 ignore)
└── quartz.config.yaml
```

템플릿: `templates/노트-공개.md`, `templates/노트-비공개.md`, `templates/노트-허브.md`, `templates/노트-stub.md`

---

## 4. 로컬에서 사이트 미리보기

```bash
npm ci
npx quartz plugin install
npm run preview:wiki
```

또는 `npm run build:wiki` → `public/` 생성. 
GitHub Actions(`deploy.yml`)도 `npx quartz build -d .` 사용.

`quartz.config.yaml`의 `baseUrl`을 본인 GitHub Pages 주소로 바꾼다.

---

## 5. 공개하면 안 되는 것

- 계좌·카드 번호, 주민·주소, 가족 실명 조합
- 보험 증권 전문, 가입설계 견적 개인분
- 회사 비공개·고객 데이터
- API 키, 비밀번호
- `private/`에 둘 내용

교육용 일반론(보험 기초, 케이패스 개요 등)은 공개해도 되고, **내 계약 숫자**는 비공개.

경고: 저장소가 **public**이면 `publish: false` 노트도 GitHub에 보인다. 
비밀은 `private/`(gitignore) 또는 **private 저장소**.

---

## 6. 공개 글 표기 (범용)

`publish: true` 글은 **웹·GitHub에 그대로** 노출된다. 독자·검색엔진·광고 검증 봇이 읽는다.

### 넣지 않는 것 (공개 본문)

| 구분 | 예 (쓰지 않음) |
|------|----------------|
| 본인 식별 | GitHub 사용자명, 실제 Pages URL, 커스텀 도메인, 내부 저장소 이름 |
| 계정·키 | AdSense `pub-` 실값, API 키, 토큰, 비밀번호 |
| 1인칭 소유 | 「이 vault」「내 사이트」「우리 위키」— 절차·설정은 **도구 일반론**으로 |
| 운영 실태 | 「AdSense에 등록한 도메인은 ○○」처럼 **본인만 아는** 배포 상태 |

개인·운영 메모는 **`공개 규칙.md`(비공개)**·`private/`·로컬에만 둔다.

### 대신 쓰는 것

| 구분 | 예 |
|------|-----|
| 플레이스홀더 | `username.github.io`, `repo-name`, `example.com`, `pub-XXXXXXXXXXXXXXXX` |
| 일반 주어 | 「Quartz + GitHub Pages 위키」「프로젝트 Pages」「루트 도메인 저장소」 |
| 절차 글 | 플랫폼·도구 **공통 단계**—독자가 자기 환경에 맞게 치환 |

### 적용 예

- **좋음:** `baseUrl`을 `username.github.io/repo-name` 형식으로 맞춘다 ([[Quartz]]).
- **나쁨:** `baseUrl`이 `jwjhek.github.io/saeng-hwal-wiki`와 일치하는지 확인한다.
- **좋음:** [[Google AdSense]] — GitHub Pages 루트·프로젝트 경로 이중 구조.
- **나쁨:** AdSense 사이트 URL은 `jwjhek.github.io`로 등록했다.

새 글·기존 글 수정 시 `publish: true`이면 위 표를 한 번 점검한다.

---

## 7. 체크리스트 (새 글)

- [ ] 분류 폴더에 작성 
- [ ] `depth` 지정 (`hub`만 목차 불릿 추가, `stub`은 허브 표만) 
- [ ] 상단 `분류` 인용 + frontmatter (`publish` / `draft`) 
- [ ] 한자 없이 한글·필요 시 영어 (고유명·법령 인용 등만 예외) 
- [ ] 공개 본문에 **본인 URL·계정 ID·저장소명** 없음 → §6 플레이스홀더·범용 표기 
- [ ] 민감 정보 없음 → `publish: true` 
- [ ] 민감·미완 → `publish: false` 또는 `draft: true` 
- [ ] (선택) `npm run preview:wiki`로 링크 확인 

---

## 관련

- [[생활위키 목차]]
- [[Quartz]]
- [[Obsidian 사용법]]
- [[Cursor 사용법]]
